apiVersion: v1
kind: Service
metadata:
  labels:
    service: actual-budget
  name: actual-budget
  namespace: actual-budget
spec:
  type: LoadBalancer
  selector:
    service: actual-budget
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 5006 #Container port